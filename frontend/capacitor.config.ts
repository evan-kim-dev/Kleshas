const config = {
  appId: "com.kleshas.app",
  appName: "번뇌",
  webDir: "out",
  server: {
    androidScheme: "https",
    iosScheme: "https",
  },
  ios: {
    contentInset: "automatic",
    scrollEnabled: false,
    allowsLinkPreview: false,
  },
};

export default config;
